package com.danish.spring.capstone;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// ============================================================================
// 70 - CAPSTONE: BUILDING A COMPLETE REST API END TO END
// ============================================================================
// A Task Management API combining nearly every concept from this curriculum:
// Spring Data JPA (05), Bean Validation (04), global exception handling (04),
// JWT authentication (07), transactions (09), caching (10), pagination (04).
// Run: mvn -f Spring/14-capstone/70-building-a-complete-rest-api-end-to-end spring-boot:run
// ============================================================================
@SpringBootApplication
public class CapstoneApplication {
    public static void main(String[] args) {
        SpringApplication.run(CapstoneApplication.class, args);
    }
}
