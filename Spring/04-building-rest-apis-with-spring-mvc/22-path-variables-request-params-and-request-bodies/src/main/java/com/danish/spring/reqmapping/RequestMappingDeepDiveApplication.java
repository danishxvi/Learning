package com.danish.spring.reqmapping;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// ============================================================================
// 22 - PATH VARIABLES, REQUEST PARAMS AND REQUEST BODIES
// ============================================================================
// Run: mvn -f Spring/04-building-rest-apis-with-spring-mvc/22-path-variables-request-params-and-request-bodies spring-boot:run
// See the .md for the exact curl commands this lesson runs against it.
// ============================================================================
@SpringBootApplication
public class RequestMappingDeepDiveApplication {
    public static void main(String[] args) {
        SpringApplication.run(RequestMappingDeepDiveApplication.class, args);
    }
}
