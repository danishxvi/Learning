spring.application.name=eureka-server
server.port=8761

# This server is a standalone Eureka instance for this lesson - it doesn't register
# itself as a client of itself, and doesn't fetch a registry from anywhere else.
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
