# A SECOND properties file, deliberately NOT named application.properties, so Spring Boot
# would never load it automatically. @PropertySource on AppInfo.java is what pulls it in.
app.version=3.2.1
app.build-name=river-otter
