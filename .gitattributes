# Normalise line endings so the repo is identical on Windows, macOS and Linux.
* text=auto eol=lf

# Source files are UTF-8; treat them as text everywhere.
*.java  text
*.js    text
*.jsx   text
*.ts    text
*.tsx   text
*.html  text
*.css   text
*.json  text
*.md    text
*.xml   text
*.yml   text
